<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Raleway">
    <title>Edit <%= account.accountNumber %> </title>
</head>

<body>

    <h1>Edit Account <%= account.accountNumber %></h1>

    <hr>
    <form action="/accounts/<%= account._id%>?_method=PUT" method="POST">
        <!-- Account Number section -->
        <div class="form-group col-md-2">
            <label for="accountNumber">Account Number</label>
            <input type="text" name="accountNumber" class="form-control" value=" <%= account.accountNumber %>" required>

        </div>
        <!-- Bank name section -->
        <div class="form-group col-md-2">
            <label for="institution">Bank Name:</label>
            <input type="text" name="institution" class="form-control" value=" <%= account.institution %>" required>
        </div>
        <!-- Account Type section -->
        <div class=" form-group col-md-2">
            <label for="accountType">Account Type</label>
            <select name="accountType" class="form-control" value=" <%= account.accounttype %>" required>
                <option selected> Choose...</option>
                <option>Checking</option>
                <option>Saving</option>
            </select> <br>

        </div>
        <div class="form-group col-md-2">
            <label for="balance">Balance:</label>
            <input type="text" name="balance" class="form-control" value="<%= account.balance%>" required>
        </div>

        <!-- Account Purpose section -->
        <div class="form-group col-md-2">
            <label for="description">Account Purpose:</label>
            <textarea name="description" class="form-control" cols="30" rows="1"><%= account.description %> </textarea>

        </div> </br>
        <!-- The create button section -->
        <div class="form-group col-md-2">
            <button type="submit" class="btn btn-primary">Update</button>
        </div>
    </form>


</body>

</html>